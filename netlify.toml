[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback — serve index.html for any unmatched route (static assets win first)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

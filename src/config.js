// ─── Brand / growth config ──────────────────────────────────────────────────
// Update these when you register the real domain + socials. They appear on the
// shareable Aura Card and in referral links — every shared screenshot becomes
// an ad, so keep them short and memorable.

export const APP_NAME = 'Text Aura';
export const APP_URL = 'https://textaura.app'; // TODO: point at your real domain
export const HANDLE = '@textaura';

// Referral
export const REFERRAL_BONUS = 10; // bonus shifts a new user gets from an invite link
